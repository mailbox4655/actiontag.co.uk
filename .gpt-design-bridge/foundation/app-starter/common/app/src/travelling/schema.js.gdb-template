export const TRAVELLING_SCHEMA_VERSION = 1;
export const TRAVELLING_MIGRATION_NAME = "001_tasks.sql";
export const TRAVELLING_SCHEMA_SQL = `
CREATE TABLE tasks (
  id TEXT PRIMARY KEY,
  title TEXT NOT NULL CHECK (length(title) BETWEEN 1 AND 160),
  completed INTEGER NOT NULL DEFAULT 0 CHECK (completed IN (0, 1)),
  created_at TEXT NOT NULL
);
CREATE INDEX tasks_created_at_id_idx ON tasks(created_at, id);
`.trim();

export const SPECIMEN_TASKS = Object.freeze([
  Object.freeze({
    id: "00000000-0000-4000-8000-000000000101",
    title: "SPECIMEN — review the mobile navigation",
    completed: true,
    createdAt: "2026-08-01T09:00:00.000Z",
  }),
  Object.freeze({
    id: "00000000-0000-4000-8000-000000000102",
    title: "SPECIMEN — verify the empty state",
    completed: false,
    createdAt: "2026-08-01T09:05:00.000Z",
  }),
]);
